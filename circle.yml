machine:
  timezone:
    Asia/Tokyo
  services:
    - docker

dependencies:
  pre:
    - pwd
    - ls -al
  override:
    - docker build -t masashi0127/cake-ddd .

test:
  override:
    - docker run -ti masashi0127/cake-ddd "cd /var/www/html"
