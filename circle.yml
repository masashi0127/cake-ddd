machine:
  timezone:
    Asia/Tokyo
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t masashi0127/cake-ddd .
  post:
    - docker run -d masashi0127/cake-ddd /bin/bash

test:
  override:
    - echo 'ok'
