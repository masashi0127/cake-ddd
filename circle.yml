machine:
  timezone:
    Asia/Tokyo
  services:
    - docker

dependencies:
  pre:
    - pwd
    - ls -al
  override:
    - docker info
    - docker build -t masashi0127/cake-ddd .
  post:
    - docker run -m 1024m -d masashi0127/cake-ddd /bin/bash

test:
  override:
    - echo 'ok'
